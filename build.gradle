import static com.yworks.yfiles.api.generator.Generator.generateKotlinWrappers

group 'com.yworks.yfiles'
version '2.0.1.4-SNAPSHOT'

buildscript {
    ext.kotlin_version = '1.2.10'
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

apply plugin: 'kotlin2js'

clean.doLast {
    delete 'web'
}

compileKotlin2Js {
    kotlinOptions {
        outputFile = "${projectDir}/web/index.js"
        moduleKind = 'amd'
        metaInfo = false
        sourceMap = false
    }
}

task generateSources() {
    def apiPath = "https://docs.yworks.com/yfileshtml/assets/api.04860ba1.js"
    generateKotlinWrappers(apiPath, new File(projectDir, 'src/main/kotlin'))
}

compileKotlin2Js.dependsOn generateSources

sourceSets {
    main.kotlin.srcDirs += 'src/main/kotlin'
}

repositories {
    mavenCentral()
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib-js:$kotlin_version"
}